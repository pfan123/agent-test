- `skills` 教大模型如何处理数据，俗称“说明文档”，保证大模型稳定输出约束的结果，其实跟 `pattern` 有点类似。

- `MCP` 给大模型供给数据（程序）

Agentic RAG (智能体驱动的检索增强工具)

rag 知识库提前向量化。

A2A，agent to agent 通信协议。

MCP 与 A2A 区别，作用域不同：

MCP 协议作用在 agent 内部

A2A 协议是作用在 Agent 与 Agent 之间。

